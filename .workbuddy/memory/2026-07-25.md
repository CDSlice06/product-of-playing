# 2026-07-25

## 已完成工作

- 启动项目 Vite 开发服务器（端口 5173），交付访问链接。
- 按用户截图，将 `src/pages/Auth.tsx` 登录/注册弹窗从 `top: 30vh` 调整为 `top: 39vh`（向下移动 30%），其它元素位置未改动。
- 处理标题图：编写 `scripts/remove_white_bg.py`，使用 PIL flood-fill，从四角出发只把与白色背景连通的像素设为透明，阈值 240，**不做羽化**。用户先后通过剪贴板提供过 5 张图（最后一次 MD5 与倒数第 2 次完全相同 `e14...1e`，且 PIL 检测为 RGB 而非 RGBA、四角均为 (255,255,255)），都按同样流程处理：
  - 1231x628 横向 → `lobby-title-v3.png`（已删除）
  - 1920x1920 方图（含"豆包"水印）→ `lobby-title-v4.png`（已删除）
  - 1920x1080 16:9 无水印版 → `lobby-title-v5.png`（已删除）
  - 同上 1920x1080（缓存破除）→ `lobby-title-v6.png`（已删除）
  - 同上 1920x1080（最新一次上传，仍是 RGB）→ 用户认为它已是透明，实际仍需要处理，输出到 `lobby-title-v7.png`（最终采用）
- `src/constants/assets.ts` 中 `LOBBY_TITLE_IMG` 指向 `lobby-title-v7.png`。
- `src/pages/Auth.tsx` 顶部 `<img>` 引用 `LOBBY_TITLE`，固定 `top: 2vh`、宽度 36rem（适配 16:9 横向版式），水平居中，`z-20` 浮于背景视频与登录弹窗之上；其它组件（含 `top: 39vh` 的登录弹窗）未改动。

## 备注

- 项目通过 Vite dev server 提供前端，预览地址 http://localhost:5173。
- 前端 `/api` 代理到 `http://localhost:3001` 后端服务，需要时另行启动 server/。
- 未来若需重新生成透明背景图，运行 `python scripts/remove_white_bg.py` 即可。阈值用 240，去掉羽化。
- 标题图经过多次迭代：处理前必须先核对用户实际给的图（同一标题图出现过 1231x628 / 1920x1920 / 1920x1080 三个版本），不要假设本地 assets 中的旧版本就是用户的新图。
- **Canvas 全项目重写**：新建 `canvas-project/`，纯 Canvas + 原生 JS，无 React、无联网模块、无 Supabase。
  - 目录：`index.html`（主入口）、`engine/`（constants.js + game.js + ai.js + tarot.js）、`assets/`（7张素材）
  - 5个场景：Auth（登录/游客）、Lobby（大厅选择）、Battle（六边形棋盘对战+AI）、Divination（78张塔罗占卜）、Gallery（9张战斗卡牌图鉴）
  - 服务器端口 8888（Python http.server）
